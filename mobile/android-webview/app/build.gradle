plugins {
  id 'com.android.application'
}
android {
  namespace 'com.launchquality.staff'
  compileSdk 34
  defaultConfig {
    applicationId 'com.launchquality.staff'
    minSdk 24
    targetSdk 34
    versionCode 7040
    versionName '70.4.0'
    buildConfigField "String", "APP_URL", "\"https://web-production-08d73.up.railway.app/app.html?field=1\""
  }
  buildFeatures { buildConfig true }
  signingConfigs {
    release {
      storeFile file("/workspace/tools/android/launchquality-release.jks")
      storePassword 'launchquality'
      keyAlias 'launchquality'
      keyPassword 'launchquality'
    }
  }
  buildTypes {
    release {
      minifyEnabled false
      signingConfig signingConfigs.release
    }
    debug {
      signingConfig signingConfigs.release
    }
  }
  compileOptions {
    sourceCompatibility JavaVersion.VERSION_17
    targetCompatibility JavaVersion.VERSION_17
  }
}
dependencies {
  implementation 'androidx.appcompat:appcompat:1.6.1'
  implementation 'com.google.android.material:material:1.11.0'
}
