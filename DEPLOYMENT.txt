Launch Quality LLC v70.4-finish-remaining — Production

Repo: https://github.com/walednajjar2-salam/jawdah-cloud-v47
Live: https://web-production-08d73.up.railway.app/fresh
Health: https://web-production-08d73.up.railway.app/api/health
Expected version: Launch-Quality-LLC-v70.4-finish-remaining
Stable tag: v70.4-complete

1) Deploy from GitHub branch `main`
2) Railway Volume mounted at:
   /app/data
3) Core variables:
   JAWDAH_HOST=0.0.0.0
   JAWDAH_DATA_DIR=/app/data
   JAWDAH_BACKUP_DIR=/app/data/backups
   JAWDAH_AUTO_BACKUP=1
   JAWDAH_BACKUP_INTERVAL_HOURS=24
   JAWDAH_BACKUP_RETENTION=30
   JAWDAH_STORAGE_WARN_GB=2
   ADMIN_PASSWORD=<secure-password>
4) Optional (owner dashboard):
   Railway Bucket → Variable References (AWS SDK) → Redeploy
   DATABASE_URL or LQ_DATABASE_URL (Postgres shadow only)
   LQ_OFFSITE_BACKUP_URL (webhook on top of local Volume mirror)
5) Verify:
   /api/health  -> version contains v70.4, ok=true, platform_ready=true
   /fresh       -> clear cache then login
   /get-windows -> EXE + portable ZIP
   /get-android -> staff APK
6) Smoke flow:
   portal select -> estate / majlis / accounting hubs
   majlis event book OR estate contract -> invoice -> payment -> backup

Start command: python server.py
Health check: /api/health

Policy: do not reset existing user passwords on deploy.
